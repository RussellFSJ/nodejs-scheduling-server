const { cleaning, docking, localise, navigate } = require("./ros");
const sleep = require("./sleep");

const cleaningProcess = (cleaning_plan) => {
    let 
}
// docking("undock");
// sleep(60000);
// localise();
// sleep(30000);
// cleaning(cleaning_plan);
// navigate(JSON.parse(process.env.HOME_ZONES)[cleaning_plan]);
// docking("dock");

module.exports = cleaningProcess;